import XCTest
@testable import NetworkingAsyncAwaitPackage

final class NetworkingAsyncAwaitPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NetworkingAsyncAwaitPackage().text, "Hello, World!")
    }
}
